spring:
  profiles:
    active: dev
  batch:
    job:
      enabled: false
mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.github.xuyh.batchservice.core.mapper
  executor-type: batch